@using IBS.Utility.Constants
@model IEnumerable<MobilityBankAccount>

@{
    ViewData["Title"] = "Bank Account";
}

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">@ViewData["Title"]</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4 table-responsive">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-area="Mobility" asp-controller="BankAccount" asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Create New
                </a>
            </div>
        </div>
        <table class="table table-bordered table-striped" id="dataTable">
            <thead>
            <tr class="text-nowrap">
                <th class="text-start">
                    @Html.DisplayNameFor(model => model.AccountNo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.AccountName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Bank)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Branch)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CreatedBy)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CreatedDate)
                </th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @foreach (var data in Model)
            {
                <tr>
                    <td class="text-start">
                        @Html.DisplayFor(modelItem => data.AccountNo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => data.AccountName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => data.Bank)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => data.Branch)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => data.CreatedBy)
                    </td>
                    <td>
                        @data.CreatedDate.ToString(SD.Date_Format)
                    </td>
                    <td>
                        <div class="w-100 btn-group" role="group">
                            <a asp-area="Mobility" asp-controller="BankAccount" asp-action="Edit" asp-route-id="@data.BankAccountId" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-square"></i> Edit
                            </a>
                        </div>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
